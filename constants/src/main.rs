fn main() {
	const MAX_POINTS: u32 = 100_000;
	let points = 20_000;
	println!("const MAX_POINTS: {:?}", MAX_POINTS);
	println!("let points: {:?}", points);

	let _unused_variable = 3u32; // example of unused variable
}
